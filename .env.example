# Postgres connection string (Neon, Supabase, local Docker, etc.)
# Neon: copiază "Connection string" din dashboard (include ?sslmode=require)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
